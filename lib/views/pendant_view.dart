import 'package:flutter/material.dart';

class PendantView extends StatelessWidget {
  static const String name = " pendant_view";
  const PendantView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pendiente"),
    );
  }
}